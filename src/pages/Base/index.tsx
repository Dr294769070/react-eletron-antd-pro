const Base: React.FC = () => {
    return <div>
        <div>第一个需求</div>
        <div>启动nginx/关闭nginx进程</div>
        <div>第二个需求</div>
        <div>动态改变host配置</div>
        <div>可以定义两个常用模板，在两者中切换</div>
    </div>
};

export default Base;